# AI Digest — direct Python dependencies (repo .venv)
#
# New machine (recommended):
#   python admin/manage.py bootstrap
#
# Exact pin match (reproducible, from last green bootstrap + tests):
#   python admin/manage.py bootstrap --locked
#
# Refresh lock after dependency changes:
#   python admin/manage.py freeze-requirements
#
# Manual:
#   uv venv && uv pip install -r requirements.txt && uv run playwright install chromium
#   pip install -r requirements.txt && playwright install chromium
#
# Hermes agent CLI is separate (~/.hermes/hermes-agent); see agentic/hermes/POC.md.
# Agentic synthesize_digest uses this repo .venv (instructor + llm_pipeline).

pyyaml>=6.0.3
pydantic>=2.13.0

# Structured LLM output (Ollama OpenAI-compatible API; enrich + agentic synthesizer)
instructor>=1.15.0
openai>=2.44.0

# Leaderboard ingestion (Playwright-backed crawl4ai)
crawl4ai>=0.9.0
playwright>=1.61.0

# YouTube chapter extraction (preflight)
yt-dlp>=2026.7.4
