# AI Digest Python dependencies
# Install: pip install -r requirements.txt && playwright install chromium

pyyaml>=6.0.1
pydantic>=2.7.0

# Structured LLM output (Ollama OpenAI-compatible API)
instructor>=1.4.0
openai>=1.30.0

# Leaderboard ingestion (Playwright-backed)
crawl4ai>=0.4.0
playwright>=1.40.0

# YouTube chapter extraction (preflight)
yt-dlp>=2024.4.9
