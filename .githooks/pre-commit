#!/bin/sh
# Three-layer security gate: fast rules → Presidio PII → secret scanner.
set -eu
cd "$(dirname "$0")/.."
python3 scripts/check_secrets.py --staged
python3 scripts/audit_pii.py --staged
python3 scripts/audit_secrets.py --staged
