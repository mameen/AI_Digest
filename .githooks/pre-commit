#!/bin/sh
# Secret scanner gate: Betterleaks/Gitleaks (or detect-secrets fallback) on staged files.
set -eu
cd "$(dirname "$0")/.."
. "$(dirname "$0")/python-venv.sh"
"$PY" scripts/audit_secrets.py --staged
