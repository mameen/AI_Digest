# Test Matrix

1. empty source list -> graceful no-story output
2. malformed feed item -> source marked failed
3. duplicate stories across two sources -> one card retained
4. invalid output schema -> publish blocked
5. broken url format -> item rejected
